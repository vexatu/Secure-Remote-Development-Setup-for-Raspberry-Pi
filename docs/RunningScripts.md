# Python Scrpits Inside .venv

This file is dedicated to showcase a practical example.

In my case I will use my RaspberryPi and a DHT22 temperature and humidity sensor and I want to initialize the sensor and print the sensor readings in my terminal.

I installed all the libraries that I need for this example, using pip.

`pip install adafruit-circuitpython-dht`

`pip install `
